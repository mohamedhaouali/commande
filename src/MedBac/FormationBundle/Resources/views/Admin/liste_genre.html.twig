{% extends "MedBacFormationBundle::layout_admin.html.twig" %}
{%block content%}
<div  style=" height:600px" class="cont_center">
{% block notice %}
	<div id="templatemo_main">
            <ul>
             {% for flashMessage in app.session.flashbag.get('message') %}

<div class="success"  >  
    <p style="color:red"> {{ flashMessage }}</p>

</div> 
        
{% endfor %}
            </ul>
         {%endblock%}
                <center> <h2>Liste des Genres</h2> </center><br><br>
                <center>
                    <table class="table table-hover table-responsive table-bordered" style="margin-top:70px;">
        <thead>
            <tr>
                 <th>Libelle Genre</th>
                
                <th>Modifier</th>
                <th>Supprimer</th>
               
             </tr>
        </thead>
        <tbody>
 {% for entity in genre.currentPageResults %}
 <tr>
             <td>{{ entity.libelle }}</td>
              <td>    <a href="{{path('genre_modifier',{ 'id': entity.id })}}"><img src="{{ asset('bundles/medbacformation/images/modifier.gif') }}" alt="" width="52" height="26" border="0" class="add_to_cart" /></a>
              </td>
              <td>       <a href="{{path('genre_supprimer',{ 'id': entity.id })}}"><img src="{{ asset('bundles/medbacformation/images/c7.png') }}" alt="" width="42" height="26" border="0" class="add_to_cart" onclick="return confirm('Etes vous sur de vouloir supprimer cet Genre ?');" /></a>
              </td>
 </tr>
                {% else %}
        <li>Aucun Bijou trouvé.</li>
{%endfor%}
            </tbody>
    </table> </center>
            <div class="pagerfanta pull-right" ><br><br><br><br>
                <center>{{ pagerfanta(genre,'default_translated') }}</center>
        </div>
            </div>
{%endblock%}
